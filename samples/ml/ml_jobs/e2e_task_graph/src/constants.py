import os

ROLE_NAME = os.getenv("SNOWFLAKE_ROLE") or "ENGINEER"
DB_NAME = os.getenv("SNOWFLAKE_DATABASE") or "SNOWBANK"
SCHEMA_NAME = os.getenv("SNOWFLAKE_SCHEMA") or "DEV"
WAREHOUSE = os.getenv("SNOWFLAKE_WAREHOUSE") or "DEMO_WH"
COMPUTE_POOL = os.getenv("SNOWFLAKE_COMPUTE_POOL") or "DEMO_POOL"

DAG_STAGE = f"@{DB_NAME}.{SCHEMA_NAME}.DAG_STAGE"
JOB_STAGE = f"@{DB_NAME}.{SCHEMA_NAME}.JOB_STAGE"

FEATURE_STORE_NAME = os.getenv("FEATURE_STORE_NAME") or f"{DB_NAME}.DATA"
DATA_TABLE_NAME = (
    os.getenv("DATA_TABLE_NAME") or f"{DB_NAME}.DATA.MORTGAGE_LENDING_DEMO_DATA"
)
