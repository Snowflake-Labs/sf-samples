SELECT 
  COMPANY_CODE, 
  DOCUMENT_TYPE, 
  DOCUMENT_NUMBER, 
  POSTING_DATE, 
  CLEARING_DATE, 
  DEBIT_AMT_LOCAL_CURRENCY, 
  CLEARING_DATE IS NOT NULL AS STATUS, 
  DATEDIFF(days, POSTING_DATE, CLEARING_DATE) AS DSO 
FROM 
  {{ ref('accounts_receivable') }}
WHERE DOCUMENT_TYPE='RV'
