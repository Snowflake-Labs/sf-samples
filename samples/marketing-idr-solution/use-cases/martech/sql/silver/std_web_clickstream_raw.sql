-- ============================================================================
-- Martech: SILVER.STD_WEB_CLICKSTREAM_RAW
-- Anonymous and logged-in events; *_STD columns populated when present.
-- ============================================================================

CREATE TABLE IF NOT EXISTS IDENTIFIER('&{deployment_db}.SILVER.STD_WEB_CLICKSTREAM_RAW') (
    -- Original bronze columns
    EVENT_ID              VARCHAR,
    TS                    TIMESTAMP_NTZ,
    ANONYMOUS_ID          VARCHAR,
    DEVICE_ID             VARCHAR,
    UID2                  VARCHAR,
    RAMPID                VARCHAR,
    SESSION_ID            VARCHAR,
    USER_AGENT            VARCHAR,
    IP                    VARCHAR,
    EVENT_NAME            VARCHAR,
    PAGE_URL              VARCHAR,
    LOGGED_IN_EMAIL       VARCHAR,
    LOGGED_IN_PHONE       VARCHAR,
    LOGGED_IN_MEMBER_ID   VARCHAR,
    EVENT_PROPERTIES_JSON VARIANT,
    SOURCE_FILE           VARCHAR,
    INGESTED_AT           TIMESTAMP_NTZ,
    -- Standardized identifier columns
    ANONYMOUS_ID_STD      VARCHAR,
    DEVICE_ID_STD         VARCHAR,
    UID2_STD              VARCHAR,
    RAMPID_STD            VARCHAR,
    EMAIL_STD             VARCHAR,
    EMAIL_HEM             VARCHAR,
    PHONE_E164            VARCHAR,
    LOGGED_IN_MEMBER_ID_STD VARCHAR,
    USER_AGENT_STD        VARCHAR,
    IP_STD                VARCHAR,
    -- Prefixed *_STD shadows: the engine identifier extractor probes
    -- <bronze_col>_STD (LOGGED_IN_EMAIL_STD, ...). SP_CUSTOM_STANDARDIZE writes them.
    LOGGED_IN_EMAIL_STD   VARCHAR,
    LOGGED_IN_PHONE_STD   VARCHAR,
    -- v1: ML blocking attributes (note: WEB has no name/postal — only EMAIL + PHONE attrs apply)
    EMAIL_HANDLE_NORMALIZED  VARCHAR,
    SOUNDEX_LNAME            VARCHAR,
    PHONE_LAST10             VARCHAR,
    PHONE_LAST7              VARCHAR,
    PHONE_AREA_CODE          VARCHAR,
    IS_BLOCKABLE_EMAIL       BOOLEAN,
    IS_BLOCKABLE_PHONE       BOOLEAN,
    ML_BLOCKING_PROCESSED    BOOLEAN DEFAULT FALSE,
    ML_BLOCKING_PROCESSED_AT TIMESTAMP_NTZ,
    -- Processing metadata
    STD_PROCESSED_AT      TIMESTAMP_NTZ,
    STD_BATCH_ID          VARCHAR,
    IDR_PROCESSED         BOOLEAN DEFAULT FALSE,
    IDR_PROCESSED_AT      TIMESTAMP_NTZ,
    METADATA_ACTION       VARCHAR DEFAULT 'INSERT',
    METADATA_ISUPDATE     BOOLEAN DEFAULT FALSE
);

-- Idempotent migration for existing deployments.
ALTER TABLE IF EXISTS IDENTIFIER('&{deployment_db}.SILVER.STD_WEB_CLICKSTREAM_RAW') ADD COLUMN IF NOT EXISTS EMAIL_HANDLE_NORMALIZED  VARCHAR;
ALTER TABLE IF EXISTS IDENTIFIER('&{deployment_db}.SILVER.STD_WEB_CLICKSTREAM_RAW') ADD COLUMN IF NOT EXISTS SOUNDEX_LNAME            VARCHAR;
ALTER TABLE IF EXISTS IDENTIFIER('&{deployment_db}.SILVER.STD_WEB_CLICKSTREAM_RAW') ADD COLUMN IF NOT EXISTS PHONE_LAST10             VARCHAR;
ALTER TABLE IF EXISTS IDENTIFIER('&{deployment_db}.SILVER.STD_WEB_CLICKSTREAM_RAW') ADD COLUMN IF NOT EXISTS PHONE_LAST7              VARCHAR;
ALTER TABLE IF EXISTS IDENTIFIER('&{deployment_db}.SILVER.STD_WEB_CLICKSTREAM_RAW') ADD COLUMN IF NOT EXISTS PHONE_AREA_CODE          VARCHAR;
ALTER TABLE IF EXISTS IDENTIFIER('&{deployment_db}.SILVER.STD_WEB_CLICKSTREAM_RAW') ADD COLUMN IF NOT EXISTS IS_BLOCKABLE_EMAIL       BOOLEAN;
ALTER TABLE IF EXISTS IDENTIFIER('&{deployment_db}.SILVER.STD_WEB_CLICKSTREAM_RAW') ADD COLUMN IF NOT EXISTS IS_BLOCKABLE_PHONE       BOOLEAN;
ALTER TABLE IF EXISTS IDENTIFIER('&{deployment_db}.SILVER.STD_WEB_CLICKSTREAM_RAW') ADD COLUMN IF NOT EXISTS ML_BLOCKING_PROCESSED    BOOLEAN;
ALTER TABLE IF EXISTS IDENTIFIER('&{deployment_db}.SILVER.STD_WEB_CLICKSTREAM_RAW') ADD COLUMN IF NOT EXISTS ML_BLOCKING_PROCESSED_AT TIMESTAMP_NTZ;
ALTER TABLE IF EXISTS IDENTIFIER('&{deployment_db}.SILVER.STD_WEB_CLICKSTREAM_RAW') ADD COLUMN IF NOT EXISTS LOGGED_IN_EMAIL_STD VARCHAR;
ALTER TABLE IF EXISTS IDENTIFIER('&{deployment_db}.SILVER.STD_WEB_CLICKSTREAM_RAW') ADD COLUMN IF NOT EXISTS LOGGED_IN_PHONE_STD VARCHAR;
