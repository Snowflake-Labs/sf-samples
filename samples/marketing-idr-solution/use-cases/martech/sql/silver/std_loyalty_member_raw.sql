-- ============================================================================
-- Martech: SILVER.STD_LOYALTY_MEMBER_RAW
-- ============================================================================

CREATE TABLE IF NOT EXISTS IDENTIFIER('&{deployment_db}.SILVER.STD_LOYALTY_MEMBER_RAW') (
    -- Original bronze columns
    MEMBER_ID       VARCHAR,
    EMAIL           VARCHAR,
    PHONE           VARCHAR,
    FIRST_NAME      VARCHAR,
    LAST_NAME       VARCHAR,
    STREET_ADDRESS  VARCHAR,
    CITY            VARCHAR,
    STATE           VARCHAR,
    POSTAL_CODE     VARCHAR,
    COUNTRY         VARCHAR,
    TIER            VARCHAR,
    POINTS          NUMBER,
    ENROLLED_AT     TIMESTAMP_NTZ,
    SOURCE_FILE     VARCHAR,
    INGESTED_AT     TIMESTAMP_NTZ,
    -- Standardized identifier columns
    EMAIL_STD             VARCHAR,
    EMAIL_HEM             VARCHAR,
    PHONE_E164            VARCHAR,
    MEMBER_ID_STD         VARCHAR,
    FIRST_NAME_STD        VARCHAR,
    LAST_NAME_STD         VARCHAR,
    FIRST_NAME_CANONICAL  VARCHAR,
    STREET_ADDRESS_STD    VARCHAR,
    CITY_STD              VARCHAR,
    STATE_STD             VARCHAR,
    POSTAL_CODE_STD       VARCHAR,
    -- v1: ML blocking attributes
    EMAIL_HANDLE_NORMALIZED  VARCHAR,
    SOUNDEX_LNAME            VARCHAR,
    PHONE_LAST10             VARCHAR,
    PHONE_LAST7              VARCHAR,
    PHONE_AREA_CODE          VARCHAR,
    IS_BLOCKABLE_EMAIL       BOOLEAN,
    IS_BLOCKABLE_PHONE       BOOLEAN,
    ML_BLOCKING_PROCESSED    BOOLEAN DEFAULT FALSE,
    ML_BLOCKING_PROCESSED_AT TIMESTAMP_NTZ,
    -- Processing metadata
    STD_PROCESSED_AT      TIMESTAMP_NTZ,
    STD_BATCH_ID          VARCHAR,
    IDR_PROCESSED         BOOLEAN DEFAULT FALSE,
    IDR_PROCESSED_AT      TIMESTAMP_NTZ,
    METADATA_ACTION       VARCHAR DEFAULT 'INSERT',
    METADATA_ISUPDATE     BOOLEAN DEFAULT FALSE
);

-- Idempotent migration for existing deployments (one ALTER per column — Snowflake limitation).
ALTER TABLE IF EXISTS IDENTIFIER('&{deployment_db}.SILVER.STD_LOYALTY_MEMBER_RAW') ADD COLUMN IF NOT EXISTS EMAIL_HANDLE_NORMALIZED  VARCHAR;
ALTER TABLE IF EXISTS IDENTIFIER('&{deployment_db}.SILVER.STD_LOYALTY_MEMBER_RAW') ADD COLUMN IF NOT EXISTS SOUNDEX_LNAME            VARCHAR;
ALTER TABLE IF EXISTS IDENTIFIER('&{deployment_db}.SILVER.STD_LOYALTY_MEMBER_RAW') ADD COLUMN IF NOT EXISTS PHONE_LAST10             VARCHAR;
ALTER TABLE IF EXISTS IDENTIFIER('&{deployment_db}.SILVER.STD_LOYALTY_MEMBER_RAW') ADD COLUMN IF NOT EXISTS PHONE_LAST7              VARCHAR;
ALTER TABLE IF EXISTS IDENTIFIER('&{deployment_db}.SILVER.STD_LOYALTY_MEMBER_RAW') ADD COLUMN IF NOT EXISTS PHONE_AREA_CODE          VARCHAR;
ALTER TABLE IF EXISTS IDENTIFIER('&{deployment_db}.SILVER.STD_LOYALTY_MEMBER_RAW') ADD COLUMN IF NOT EXISTS IS_BLOCKABLE_EMAIL       BOOLEAN;
ALTER TABLE IF EXISTS IDENTIFIER('&{deployment_db}.SILVER.STD_LOYALTY_MEMBER_RAW') ADD COLUMN IF NOT EXISTS IS_BLOCKABLE_PHONE       BOOLEAN;
ALTER TABLE IF EXISTS IDENTIFIER('&{deployment_db}.SILVER.STD_LOYALTY_MEMBER_RAW') ADD COLUMN IF NOT EXISTS ML_BLOCKING_PROCESSED    BOOLEAN;
ALTER TABLE IF EXISTS IDENTIFIER('&{deployment_db}.SILVER.STD_LOYALTY_MEMBER_RAW') ADD COLUMN IF NOT EXISTS ML_BLOCKING_PROCESSED_AT TIMESTAMP_NTZ;
